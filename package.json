{
  "name": "assemblyscript-benchmarks",
  "version": "1.0.0",
  "description": "Webassembly(from AssemblyScript) vs JavaScript benchmarks",
  "main": "index.js",
  "scripts": {
    "test": "node tests",
    "asbuild": "asc assembly/index.ts --target release",
    "bench": "node benchmarks"
  },
  "dependencies": {
    "@assemblyscript/loader": "^0.18.4",
    "benchmark": "^2.1.4"
  },
  "devDependencies": {
    "assemblyscript": "^0.18.4"
  }
}
